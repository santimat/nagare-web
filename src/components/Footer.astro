---
import FooterIcon from "@icons/Footer-icon.astro";
import HouseIcon from "@icons/House.svg";
import ServicesIcon from "@icons/Services.svg";
import PackagesIcon from "@icons/Packages.svg";
import FAQsIcon from "@icons/Faqs.svg";
import EmailIcon from "@icons/Email.svg";
import InstagramIcon from "@icons/Instagram.svg";

const date = new Date();
const currentYear = date.getFullYear();

const links = [
  { Icon: HouseIcon, text: "Inicio", href: "/" },
  { Icon: ServicesIcon, text: "Servicios", href: "/#services" },
  { Icon: PackagesIcon, text: "Paquetes", href: "/#packages" },
  { Icon: FAQsIcon, text: "FAQs", href: "/#faqs" },
  { Icon: EmailIcon, text: "Contacto", href: "/contact" },
  {
    Icon: InstagramIcon,
    text: "Instragram",
    href: "https://www.instagram.com/nagare.studio",
  },
];
---

<footer class="bg-footer text-accent fixed bottom-0 pt-12 text-center font-bold">
  <div class="mb-8 md:flex md:justify-evenly md:gap-8">
    <FooterIcon width="350" height="110" class="mx-auto block md:flex-1" />
    <nav class="flex-1">
      <ul class="my-4 grid justify-center gap-4 md:grid-cols-3">
        {
          links.map(({ Icon, text, href }) => (
            <li>
              <a
                class="flex items-center gap-2 text-xl"
                href={href}
                target={href.startsWith("https") ? "_blank" : "_self"}
              >
                <span>
                  <Icon />
                </span>
                {text}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
  <p class="text-text py-4">Nagare studio {currentYear}. Todos los derechos reservados</p>
</footer>
